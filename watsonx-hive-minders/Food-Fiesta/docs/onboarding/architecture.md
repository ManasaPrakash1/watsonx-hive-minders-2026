# Architecture Diagram

> Auto-generated by the onboarding-dev skill.
> Full interactive diagram: see `docs/onboarding/devOnboarding.pdf`

| Couche | Composants | Notes |
|---|---|---|
| 🌐 Client | Thymeleaf SSR, HTML Forms, CSS + JavaScript | Server-side rendering with form submissions |
| 🔒 Spring Security | OAuth2 (Google), Route authorization | ⚠️ CSRF disabled, manual session-based auth |
| 🎮 Controllers | HomeController, AdminController, UserController, ProductController, OAuth2Controller | OrderController is empty stub ⚠️ |
| ⚙️ Services | UserServices, AdminServices, ProductServices, OrderServices | ⚠️ All use @Component (not @Service), AdminServices has bug in update() |
| 🗄️ Repositories | UserRepository, AdminRepository, ProductRepository, OrderRepository | Extend CrudRepository (not JpaRepository) — findAll() returns Iterable |
| 💾 Database | H2 (in-memory, dev), PostgreSQL (optional, prod) | customer_info ── User, admin ── Admin, product_table ── Product, order_info ── Orders |
| 💡 Session | "loggedInAdmin" → String (email), "loggedInUser" → User (JPA entity) | ⚠️ Storing full JPA entity in session can cause LazyInitializationException |

## Flux de requête

`Browser → Spring Security → Controllers → Services → Repositories → Database → Response`

## Documents liés

- Onboarding complet : `docs/onboarding/devOnboarding.pdf`
- Audit de dette : `docs/audit/dette_technique.html` (sera généré automatiquement)
